#include <stdio.h>

/**
 * main - Entry - point
 *
 * Return: ALways 0 (sucess)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
